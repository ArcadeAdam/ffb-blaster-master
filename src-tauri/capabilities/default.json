{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Folder picker, the fixed support URL, and the app's own commands.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    {
      "identifier": "opener:allow-open-url",
      "allow": [
        {
          "url": "https://www.paypal.com/paypalme/acbauer12/9.99"
        }
      ]
    }
  ]
}
